CREATE TABLE IF NOT EXISTS region (
    name TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS region_id ON region(name);


selectAll:
SELECT name FROM region ORDER BY rowid;

insert:
INSERT INTO region VALUES ?;

deleteAll:
DELETE FROM region;
